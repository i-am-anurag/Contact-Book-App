const UserRepository = require('./user-repo');
const ContactRepository = require('./contact-repo');
const GroupRepository = require('./group-repo');

module.exports = {
    UserRepository,
    ContactRepository,
    GroupRepository,
}